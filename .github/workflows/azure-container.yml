# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - talentsync

on:
  push:
    branches:
      - jrdbranch
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to container registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_41EC6726B06E473BA373234BBD8466B4 }}
          password: ${{ secrets.AZUREAPPSERVICE_CONTAINERPASSWORD_CACD3DBBD0FB420EB2DE46AFB089DD01 }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: ghcr.io/${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_41EC6726B06E473BA373234BBD8466B4 }}/talentsync:${{ github.sha }}
          file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    needs: build

    steps:
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_0073A4D3917340EDA4D3139C33E931D3 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_A59EA81C4ED5493CBDD5D2948DDF7D8A }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_3CE14E1B0F9845CD925B86E25129E882 }}

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'talentsync'
          slot-name: 'Production'
          images: 'ghcr.io/${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_41EC6726B06E473BA373234BBD8466B4 }}/talentsync:${{ github.sha }}'
